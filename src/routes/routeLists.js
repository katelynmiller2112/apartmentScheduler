
import LoginPage from '../Login/Login';
// route config for app
// just array of all of the logical routes
// that will be in the app

const routes = [
    {
        path: "/home",
        component: LoginPage
    },
    
    
];

export default routes;